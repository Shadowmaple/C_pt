#include <stdio.h>

int main()
{
	int a[100][100], b[100], n, line;
	int check;

	do {
		sacnf("%d", &n);
		if (n < 2 && n <= 100)
		{
			for () {

			}
		} else if (n != 0) {
			puts("error!");
		}
	} while (n != 0);


	return 0;
}
